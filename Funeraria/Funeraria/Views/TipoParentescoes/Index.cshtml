@model IEnumerable<Funeraria.TipoParentesco>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descricao_par)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao_par)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Codigo_par }) |
            @Html.ActionLink("Details", "Details", new { id=item.Codigo_par }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Codigo_par })
        </td>
    </tr>
}

</table>
