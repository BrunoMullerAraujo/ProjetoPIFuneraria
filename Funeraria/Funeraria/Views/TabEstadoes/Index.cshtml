@model IEnumerable<Funeraria.TabEstado>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DescricaoUf_unf)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DescricaoUf_unf)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodigoUf_unf }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodigoUf_unf }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodigoUf_unf })
        </td>
    </tr>
}

</table>
